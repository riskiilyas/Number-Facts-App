import 'package:flutter/material.dart';

class DateFactScreen extends StatelessWidget {
  const DateFactScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xFF000330),
      body: SafeArea(
        child: Expanded(
          child: Column(
            children: [

            ],
          ),
        ),
      ),
    );;
  }
}
